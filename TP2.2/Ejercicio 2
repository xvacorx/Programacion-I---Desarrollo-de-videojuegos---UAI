//2. Ingresando una nota final de cursada (valor con decimales) informar si la condición es: Promovido, Regular o Libre.
//Libre: menor a 4
//Regular: mayor o igual a 4 y menor a 6
//Promovido: mayor o igual a 6.

Console.WriteLine("Ingrese nota del alumno");
float Nota = float.Parse(Console.ReadLine());

if (Nota >= 6) { Console.WriteLine("El alumno está promovido"); }
else if (Nota < 4) { Console.WriteLine("El alumno quedó libre"); }
else { Console.WriteLine("El alumno está regular"); }
